---
name: find-folder-help
description: Explain Find Folder groups, shared and local storage, GUID migration, Project window focus behavior, Unity menus, and saved-target safety boundaries.
---

# Find Folder Help

Answer in the user's language. Explain saved shortcuts without opening the tool, changing Project selection, or modifying saved targets and settings.

1. Read `PACKAGE_SKILLS.md` first. Treat its generated package identity, complete related-skill table, `$skill-name` invocations, descriptions, and access values as authoritative.
2. Read `Packages/com.actionfit.findfolder/README.md` and `AI_GUIDE.md` when available. If downloaded, resolve `Library/PackageCache/com.actionfit.findfolder@*` without editing it.
3. Explain group and nested-group shortcuts, `Assets/_Data/FindFolder/Shared/{rootGroupId}.json`, `UserSettings/FindFolder/Local/{rootGroupId}.json`, GUID-first identity, path fallback, and folder-versus-file focus behavior.
4. List `Open Window`, `Setting SO`, and `README` under `Tools > Package > Find Folder`.
5. State that this help-only skill never adds, edits, migrates, or removes saved targets; writes JSON; changes GUIDs; or focuses Project assets.
